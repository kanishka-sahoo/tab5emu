# Tab5 Retro Console firmware (ESP-IDF). The desktop build lives in host/.
cmake_minimum_required(VERSION 3.16)

set(PROJECT_VER "0.1.0")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(tab5emu)
